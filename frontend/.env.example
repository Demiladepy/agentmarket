# Local dev (Hardhat node): use these + deploy with npm run deploy:local
# VITE_LOCAL_RPC=http://127.0.0.1:8545
# Full 0x + 40 hex chars (e.g. from "npm run addresses" in contracts/)
# VITE_AGENT_REGISTRY_ADDRESS=0x...
# VITE_JOB_MARKET_ADDRESS=0x...

# Monad Testnet (when you have testnet MON): deploy with npm run deploy:testnet, then set:
# VITE_MONAD_RPC=https://testnet-rpc.monad.xyz
# VITE_AGENT_REGISTRY_ADDRESS=0x...
# VITE_JOB_MARKET_ADDRESS=0x...
