{
  "_comment": "Merge these blocks into your OpenClaw config (e.g. ~/.openclaw/openclaw.json). Replace REPLACE_WITH_ABSOLUTE_PATH with the full path to this folder (agent-marketplace).",
  "plugins": {
    "load": {
      "paths": ["REPLACE_WITH_ABSOLUTE_PATH"]
    },
    "entries": {
      "agentmarket": {
        "enabled": true,
        "config": {
          "agentServerUrl": "http://localhost:3000"
        }
      }
    }
  },
  "agents": {
    "list": [
      {
        "id": "default",
        "tools": {
          "allow": ["agentmarket"]
        }
      }
    ]
  }
}
